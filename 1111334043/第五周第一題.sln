#include <stdio.h>

int main()

{
    int weight;
    int fee;

    printf("請輸入你的包裹幾公斤：");
    scanf_s("%d", &weight);

    if (weight <= 2) {
        fee = 60;
    }
    else {
        fee = 60 + (weight - 2) * 35;
    }

    printf("運費為%d元\n", fee);

    return 0;
}
