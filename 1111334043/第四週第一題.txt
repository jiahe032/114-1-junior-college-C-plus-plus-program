#include <stdio.h>

int main()
{
	int a, b, c;
	int max,min,sun;
	printf("輸入三個整數\n");
	printf("請輸入數字一=");
	scanf_s("%d", &a);
	printf("請輸入數字二=");
	scanf_s("%d", &b);
	printf("請輸入數字三=");
	scanf_s("%d", &c);
	int w;
	float x;
	w = a + b + c;
	printf("和=%d\n",w);
	x = (a + b + c) / 3;
	printf("平均值%.2f\n",x);
	max = a;
	   if (b > max) 
               max=b;
	   if (c > max) 
               max=c;
	min = a;
	   if (b < min)  
               min=b;
	   if (c < min)  
               min=c;
	printf("最大值%d\n", max);
	printf("最小值%d\n", min);

	return 0;
	
	
}
