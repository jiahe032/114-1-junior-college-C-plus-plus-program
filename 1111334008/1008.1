#include <stdio.h>

int main(void)
{
    int a , b , c , toto;
    double avg = 0;
    char rank = 'A';

    printf("請輸入第一個數:");
    scanf_s("%d", &a);
    printf("請輸入第二個數:");
    scanf_s("%d", &b);
    printf("請輸入第三個數:");
    scanf_s("%d", &c);

    toto = a + b + c;
    printf("三者的和為:%d\n", toto);

    avg =toto/3.0;
    printf("三者的平均值為:%f\n", avg);

    //最大值
    if (a > b) {
        if (a > c) {
            printf("三者的最大值為：%d\n",a);
        }
        else
            printf("三者的最大值為：%d\n", c);
    }
    else if(b>c)
        printf("三者的最大值為：%d\n", b);
    else 
        printf("三者的最大值為：%d\n", c);

    //最小值
    if (a < b) {
        if (a < c) {
            printf("三者的最小值為：%d\n", a);
        }
        else
            printf("三者的最小值為：%d\n", c);
    }
    else if (b < c)
        printf("三者的最小值為：%d\n", b);
    else
        printf("三者的最小值為：%d\n", c);

    return 0;
}
