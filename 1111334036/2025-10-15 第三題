#include<stdio.h>

int main()
{
	int number, i;
	double pi = 0.0, sign = 1.0;
	printf("請輸入項數n：\n");
	scanf_s("%d", &number);
	for (i = 1; i <= number * 2; i++)
	{
		if (i % 2 == 0)
			continue;
		pi = pi + sign * (4.0 / i);
		sign = -sign;
	}
	printf("pi的近似值是：%.6f\n", pi);
}
