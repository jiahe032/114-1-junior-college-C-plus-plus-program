#include <stdio.h>
int main()
{
	int a, b, c, d;
	double e;
		printf("請輸入第一個數字:\n");
		scanf_s("%d", &a);
		printf("請輸入第二個數字:\n");
		scanf_s("%d", &b);
		printf("請輸入第三個數字:\n");
		scanf_s("%d", &c);
		d = a + b + c;
		printf("他們的和為:%d\n",d);
		e = (float)(a + b + c) / 3;
		printf("他們的平均值為:%f\n", e);
		int max = a;
			if (b > max)
				max = b;
			if (c > max)
				max = c;
			printf("最大值為:%d\n", max);
	    int small = a; 
		    if (b < small)
				small = b;
			if (c < small)
				small = c;
			printf("最小值為:%d\n", small);
	return 0;
}
