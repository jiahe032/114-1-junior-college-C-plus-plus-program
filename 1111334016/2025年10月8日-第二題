題目2. 建立一個BMI計算器應用程式，讀取使用者的體重（公斤）和身高（公尺），然後計算並顯示使用者的身體質量指數（BMI）。此外，利用if, else if 指令顯示BMI的等級，if()裡面只能使用單一運算式或條件式

#include <stdio.h>

int main()
{
	float kgk = 0, m = 0, mbti = 0;
	printf_s("請輸入你的體重(公斤)\n");
	scanf_s("%f", &kgk);
	printf_s("請輸入你的身高(公尺)\n");
	scanf_s("%f", &m);
	mbti = kgk / (m * m);
	printf_s("是%f",mbti);
	if (mbti <18.5) {
		printf_s("體重過輕\n");
	}
	else if (mbti < 25.0) {
		printf_s("正常體重\n");
	}
	else if (mbti < 30.0) {
		printf_s("過重\n");
	}
	else if (mbti < 35.0) {
		printf_s("輕度肥胖\n");
	}
	else if (mbti < 40.0) {
		printf_s("中度肥胖\n");
	}
	else if (mbti >= 40.0) {
		printf_s("重度肥胖(病態肥胖)\n");
	}
	return 0;









}
