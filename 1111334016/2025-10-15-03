題目3:請輸入一個整數表示所需的項目數量n，根據pi的近似公式pi=4-4/3+4/5-4/7+4/9-4/11+...，
利用continue指令，計算出pi的近似值

#include <stdio.h>

int main() {
    int n;
    result =4

    printf("請輸入項目數量 n：");
    scanf_s("%d", &n);

    for (int i = 1; count < n; i++) {
        if (i % 2 == 0) {
            continue;          // 跳過偶數分母
        }

        // 奇數分母才計算
        

        if (i%4==1) {
           result=result+(4.0/i);       // 偶數項加;3mod4=3
        }
        else {
           result=result-(4.0/i);       // 奇數項減
        }


    printf("根據 %d 項計算，pi 的近似值為：%.10f\n", n, result);

    return 0;
}
