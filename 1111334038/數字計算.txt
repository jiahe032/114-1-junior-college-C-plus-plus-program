#include<stdio.h>
int main(void)
{
	int num1,num2,num3,big,min ,sum ;
	double avg;
	printf("請輸入第一個數字\n");
	scanf_s("%d", &num1);
	printf("請輸入第二個數字\n");
	scanf_s("%d", &num2);
	printf("請輸入第三個數字\n");
	scanf_s("%d", &num3);
	sum = num1 + num2 + num3;
	avg = sum / 3.0;
	big = num1;
	min = num1;
	if (num2 > big)
	big = num2;
	if (num3 > big)
	big = num3;
	if (num2<min)
		min = num2;
	if (num3 <min)
		min = num3;

	printf("最大的數字是%d\n", big);
	printf("最小的數字是%d\n", min);
	printf("三個數字的和是%d\n",sum);
	printf("平均值是%f\n",avg);
	return 0;
}