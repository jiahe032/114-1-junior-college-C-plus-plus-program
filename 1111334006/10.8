#include <stdio.h>
int main(void)
{
	int a, b, c;
	int max, min;
	float sum, avg;

	printf("請輸入三整數:");
	scanf_s("%d %d %d", &a, &b, &c);

	sum = a + b + c;
	avg = sum / 3.0;

	max = a;
	if (b > max)max = b;
	if (c > max)max = c;

	min = a;
	if (b > max)min = b;
	if (c > max)min = c;

	printf("總和 = %.0\fn", sum);
	printf("平均值 = %.2\n", avg);
	printf("最大值=%d\n", max);
	printf("最小值 = %\n", min);

	return 0;

}
